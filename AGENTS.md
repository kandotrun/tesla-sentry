# AGENTS.md

## 最初に読むもの

1. `docs/PROJECT_CONTEXT.md`
2. `docs/product/tesla-sentry-ai-service-plan-2026-08-03.md`
3. `docs/TESLACAM_FORMAT_NOTES.md`
4. このファイル

実装と本書が競合する場合は、最新の受入条件と現在の実装を優先し、矛盾をドキュメントへ反映すること。

## 現在の実装優先順位

1. ブラウザ内TeslaCam manifest
2. R2への再開可能な直接アップロード
3. 1イベントのFFmpeg前処理
4. 固定Schemaの判定結果表示
5. 候補抽出とAI判定
6. 認証・課金・β運用

最初から全構成を作らない。各PRはユーザーが触れる一つの縦切りか、明確な基盤契約に限定する。

## 開発ルール

- 新しい振る舞いはRED → GREEN → REFACTORで実装する。
- テスト、型検査、Lint、ビルド、Cloudflare dry-runを該当範囲で通す。
- TypeScriptでは`any`や二重キャストで型エラーを隠さない。
- 秘密情報、Cloudflareの実ID、署名URL、実映像をコミットしない。
- 本番デプロイ、R2/D1/Queue/Container作成、課金開始は明示的なリリース判断なしに実行しない。
- 大きな動画をWorkerリクエスト本文へ通さない。将来のアップロードはR2へ直接送る。
- request固有状態をWorkerのmodule globalへ置かない。Promiseを未処理で残さない。
- UIが「ドラッグ＆ドロップ」と案内する場合は、実際のdrop処理と検証を実装する。未実装なら案内しない。

## プライバシーと安全

- 映像には顔、ナンバー、自宅・勤務先、位置、時刻が含まれる前提で設計する。
- 通常契約の映像を学習へ流用しない。学習利用は別同意と削除可能なID管理が必要。
- Tesla認証情報を要求しない。暗号化映像の非公式復号を実装しない。
- 「問題なし」は「映像上、問題を示す所見を検出しなかった」の意味であり、無損傷を保証しない。
- 不確実な映像は除外せず、`判定不能`または`要確認`へ残す。

## 言語

- ユーザー向けUI・プロダクト文書・PR説明は当面日本語。
- コード識別子と機械向けSchemaは英語。
